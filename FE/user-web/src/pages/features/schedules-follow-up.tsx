import { FeatureDetailPage } from "@/pages/features/feature-detail-page";

// 기능 : SchedulesFollowUpFeaturePage 화면을 렌더링합니다.
export function SchedulesFollowUpFeaturePage() {
  return <FeatureDetailPage featureId="schedules-follow-up" />;
}
