import { FeatureDetailPage } from "@/pages/features/feature-detail-page";

// 기능 : PipelineFeaturePage 화면을 렌더링합니다.
export function PipelineFeaturePage() {
  return <FeatureDetailPage featureId="pipeline" />;
}
