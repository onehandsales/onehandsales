# BE TODO

이번 계획의 BE 구현 작업은 없다.

현재 Backend Auth/User API 계약은 `TODO/AUTH_FE_INTEGRATION_PLAN/COMMON/AUTH-FE-CONTRACT.md`에 문서화했다. FE 작업자는 Backend 내부 구현이 아니라 해당 계약을 기준으로 구현한다.

주의:

- 깨끗한 DB에서 실제 smoke를 하려면 User/Auth DDL 또는 Prisma migration이 먼저 필요하다.
- DDL 작성은 별도 요청과 별도 계획에서 진행한다.
