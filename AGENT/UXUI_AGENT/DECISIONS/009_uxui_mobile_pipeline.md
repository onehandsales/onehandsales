# UX/UI Mobile Pipeline Decision

현재 비활성 결정이다.

2026-09-10 기준 `FE/user-web`에는 `/app/deals` 활성 route가 없고 `/app/deals/*`는 `/app`으로 redirect한다. 모바일 딜 파이프라인 UX는 현재 구현 범위에 적용하지 않는다.

딜 도메인을 다시 활성화할 때 이 파일을 새 route/API/schema 기준으로 재검토한다.
