# Contact Module

현재 비활성 문서다.

`BE/src/modules/contact`에는 런타임 TypeScript 코드가 없고 `AppModule`에도 연결되어 있지 않다. 현재 `BE/prisma/schema.prisma`에는 `Contact` 계열 모델이 남아 있지 않으며 `/api/contacts` API도 제공하지 않는다.

과거 담당자 도메인 구현 맥락이 필요할 때만 git history 또는 archived TODO 문서를 확인한다.
